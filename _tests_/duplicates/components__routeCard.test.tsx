// Archived duplicate of mergeable routeCard test. Kept here to preserve original content
// so human reviewers can inspect it. This copy is stored under _tests_/duplicates and
// will not be discovered by the main jest testMatch patterns used in CI.

// Original canonical test lives at `_tests_/core/routeCard.test.tsx`.

/* archived content below */
// Duplicate archived. Use `_tests_/core/routeCard.test.tsx` for the canonical test.
// Original comprehensive content preserved at `components__routeCard.test.orig.tsx`.
